요청하신 `analyze_system_cmd.md` 파일을 아래 형식으로 제공합니다. 구글 드라이브에 직접 파일을 생성할 수는 없으므로, 내용을 복사하여 붙여넣으실 수 있도록 마크다운 형식으로 작성했습니다.

---

```markdown
| 시스템커맨드 | Open환경에서 지원가능 여부 | 지원가능시 구현 방법 | 비고 |
|---|---|---|---|
| **COBOLG** | 지원 가능 | **GnuCOBOL 컴파일러 사용 또는 래퍼 스크립트 개발** <br> - GnuCOBOL (또는 Free COBOL)과 같은 오픈 소스 COBOL 컴파일러를 사용하여 원시 프로그램을 컴파일. <br> - `COBOLG` 명령의 다양한 파라미터(예: `SRC`, `OBJLIB`, `CPYLIB`, `FFDLIB`, `MEDLIB`, `TRACE`, `CHECK`, `MSGLNG`)를 GnuCOBOL의 컴파일러 옵션 및 환경 변수에 매핑하는 쉘 스크립트(bash/Python) 또는 C/Java 래퍼 프로그램 개발. <br> - `CPYLIB`, `FFDLIB`, `MEDLIB` 등 라이브러리 경로는 환경 변수 설정 또는 컴파일러 인클루드 경로로 지정. <br> - `OBJTYPE` (`@OBJ`, `@TEXT`, `@EOBJ`)에 따라 적절한 출력 파일 형식 지정 및 후처리. <br> - `TRACE`, `CHECK` 등 디버깅 관련 파라미터는 컴파일러의 디버깅 심볼 생성 옵션(`-g`) 및 런타임 디버거(`gdb` 또는 COBOL 디버거)와의 연동 고려. | COBOL 프로그램의 컴파일/번역을 담당. 기존 COBOL 환경의 핵심 기능 중 하나. |
| **OVRDSPF** | 부분적으로 지원 가능 (애플리케이션 계층에서 구현) | **사용자 프로그램의 파일 I/O 로직 추상화 및 재구현** <br> - Open 환경에서 디스플레이 파일 개념은 존재하지 않으므로, 기존 `DSPF` (디스플레이 파일 기술)의 역할을 대체할 웹 기반 UI (HTML/CSS/JavaScript) 또는 터미널 기반 UI (ncurses 라이브러리, Python의 `curses딕셔너리 또는 설정 파일). | 디스플레이 파일 기술을 대체하는 기능. 화면 I/O를 실제 파일 기술로 대체함. |
| **OVRPRTF** | 부분적으로 지원 가능 (애플리케이션 계층에서 구현) | **프린터 스풀링 시스템 및 리포트 생성 도구 통합** <br> - Linux의 CUPS (Common Unix Printing System)와 같은 프린터 스풀링 시스템을 활용. <br> - 기존 `PRTF` (프린터 파일 기술)의 레이아웃 및 데이터 매핑 정보를 파싱하여 CSV, JSON, XML 등 중간 데이터 형식으로 변환 후, Python의 `ReportLab` 또는 Java의 `Apache PDFBox` 등을 사용하여 PDF와 같은 표준 문서 형식으로 리포트 생성. <br> - `FILE`과 `TOFILE` 매핑 로직은 설정 파일 또는 데이터베이스로 관리하며, 파일 I/O를 CUPS의 스풀링 큐로 리다이렉션. | 프린터 파일 기술을 대체하는 기능. 인쇄 I/O를 실제 파일 기술로 대체함. |
| **CALL** | 지원 가능 | **프로세스 실행 또는 함수/메서드 호출로 매핑** <br> - COBOL 프로그램 내에서 다른 프로그램(`PGM` 파라미터)을 호출하는 경우: <br>   - 호출되는 프로그램이 OS 명령어라면 `subprocess` 모듈(Python) 또는 `system()` 함수(C)를 사용하여 실행. <br>   - 호출되는 프로그램이 다른 COBOL 프로그램이거나 C/Python/Java로 포팅된 프로그램이라면, 해당 언어의 함수/메서드 호출 또는 별도 프로세스 실행으로 매핑. <br> - 라이브러리 경로 (`LIBL` 등)는 `PATH` 환경 변수 또는 동적 라이브러리 검색 경로 설정으로 처리. | 프로그램 호출. 유연한 실행 환경을 제공함. |
| **SNDMSG** | 지원 가능 | **로그 시스템, 알림 서비스 또는 메시지 브로커 활용** <br> - 간단한 메시지 전송의 경우: `syslog` 또는 `logging` 모듈(Python)을 사용하여 표준 로그 시스템에 메시지 기록. `echo` 또는 `printf` 명령어를 사용하여 터미널에 출력. <br> - 특정 사용자(`TO` 파라미터)에게 메시지 전송의 경우: `wall` 또는 `write` 명령 (터미널 사용자에게 메시지 전송), 또는 메일/SMS API 연동. <br> - 시스템 내부 메시징의 경우: `RabbitMQ`, `Kafka` 등 메시지 브로커를 활용하여 메시지 큐에 발행/구독 모델 구현. | 메시지 전송. 프로그램 실행 중 상태나 오류를 알림. |
| **RETURN** | 지원 가능 | **프로세스 종료 또는 함수/메서드 반환** <br> - 메인 프로그램의 종료인 경우: `exit()` 함수(C), `sys.exit()`(Python), `System.exit()`(Java)를 사용하여 프로그램 종료 및 종료 코드 반환. <br> - 서브루틴/함수의 종료인 경우: 해당 언어의 `return` 문을 사용하여 호출자에게 제어권 반환. <br> - `PGMRC-?PGMEC`와 같이 종료 코드를 설정하는 경우, 프로세스 종료 시 해당 코드를 반환하도록 구현. | 호출자에게 제어권을 복귀. |
| **RESETCDR** | 지원 가능 (소프트웨어적 구현) | **시스템 시간 동기화 도구 또는 NTP 서비스 활용** <br> - `timedatectl` (systemd 기반 Linux) 또는 `ntpdate`, `chrony` 등 NTP 클라이언트를 사용하여 시스템 시계를 동기화. <br> - 애플리케이션 내부에서 사용하는 가상 캘린더/타이머가 있다면, 해당 데이터를 초기화하거나 재설정하는 로직 구현. | 캘린더 타이머 리셋. <br> - `DEV` 파라미터는 Linux에서 특정 디스크 볼륨을 의미하므로, 해당 볼륨 경로에 파일/DB 생성. <br> - **구현 언어**: Python (SQLite), C (SQLite, 파일 I/O), Shell (파일 생성, 디렉토리 관리). | 명령 분할 파일 생성. 사용자 정의 명령을 관리하기 위한 파일. |
| **DLTCPF** | 부분적으로 지원 가능 (커스텀 데이터 구조 삭제) | **파일/데이터베이스 삭제** <br> - `CRTCPF`로 생성된 파일 또는 SQLite 데이터베이스를 `rm` 명령(Shell) 또는 해당 언어의 파일/DB 삭제 API를 사용하여 삭제. | 명령 분할 파일 삭제. |
| **DSPCPF** | 부분적으로 지원 가능 (커스텀 데이터 구조 조회) | **파일/데이터베이스 내용 조회 및 출력** <br> - `CRTCPF`로 생성된 파일 또는 SQLite 데이터베이스의 내용을 읽어와 `OUTPUT` 파라미터(`@LIST`/`@DSP`)에 따라 표준 출력 또는 파일로 포맷팅하여 출력. <br> - Python의 `argparse`로 파라미터 처리, `sqlite3` 모듈로 DB 조회. | 명령 분할 파일 내용 표시. |
| **ACTCPF** | 부분적으로 지원 가능 (커스텀 데이터 구조 활성화) | **런타임 시 명령 정의 로드** <br> - `CRTCPF`로 생성된 명령 분할 파일의 내용을 애플리케이션 메모리에 로드하거나, 명령 인터프리터가 해당 파일에 접근할 수 있도록 경로를 설정. <br> - Linux `systemd` 서비스 또는 `cron` 작업을 통해 자동 로드/활성화 스크립트 실행. | 명령 분할 파일 활성화. |
| **DACTCPF** | 부분적으로 지원 가능 (커스텀 데이터 구조 비활성화) | **런타임 시 명령 정의 언로드** <br> - `ACTCPF`로 로드된 명령 정의를 메모리에서 해제하거나, 명령 인터프리터가 더 이상 해당 파일에 접근하지 않도록 경로를 해제. | 명령 분할 파일 비활성화. |
| **ADDCMD** | 부분적으로 지원 가능 (커스텀 데이터 구조에 추가) | **명령 분할 파일에 명령 정보 추가** <br> - `CRTCPF`로 생성된 데이터베이스 또는 설정 파일에 새로운 명령의 메타데이터 (이름, 권한 등)를 추가/업데이트하는 스크립트 또는 프로그램 작성. <br> - 데이터 무결성 유지를 위해 트랜잭션 처리 고려. | 명령 분할 파일에 명령 추가. |
| **DLTCMD** | 부분적으로 지원 가능 (커스텀 데이터 구조에서 삭제) | **명령 분할 파일에서 명령 정보 삭제** <br> - `ADDCMD`와 유사하게, 명령 분할 파일 (DB 또는 설정 파일)에서 특정 명령의 메타데이터를 삭제하는 스크립트 또는 프로그램 작성. | 명령 분할 파일에서 명령 삭제. |
| **ACTRETC** | 부분적으로 지원 가능 (커스텀 검색 서비스 활성화) | **명령 검색 서비스 활성화** <br> - 사용자 명령 검색 기능의 인덱스를 생성하거나, 검색 로직이 활성화되도록 설정 파일을 로드. <br> - Python의 `whoosh` 또는 `elasticsearch`와 같은 검색 라이브러리/서비스를 사용하여 검색 인덱스 구현. | 명령 검색 서비스 활성화. |
| **DACTRETC** | 부분적으로 지원 가능 (커스텀 검색 서비스 비활성화) | **명령 검색 서비스 비활성화** <br> - `ACTRETC`로 활성화된 검색 인덱스를 제거하거나, 검색 서비스를 중지. | 명령 검색 서비스 비활성화. |
| **CTLCMDLF** | 부분적으로 지원 가능 (보안 정책 관리) | **명령 사용 제한 파일 생성/관리** <br> - 이 명령은 명령 사용 제한 파일을 생성, 대체, 삭제, 표시하는 기능을 수행. <br> - **구현 방법**: <br>   - 명령 제한 규칙을 정의하는 설정 파일 (예: JSON, YAML) 포맷을 설계. <br>   - 이 설정 파일을 파싱하고, 사용자 및 그룹 정보 (`PROFILE`, `PROFG`)에 따라 명령 실행을 허용/거부하는 쉘 스크립트 또는 Python 프로그램 (`wrapper` 또는 `sudo` 정책) 개발. <br>   - `MODE` (`@CRT`, `@DLT`, `@DSP`)에 따라 파일 생성, 삭제, 표시 기능 구현. <br>   - `XUCL` 라이브러리에 `XMJCMDLF` 파일로 저장된다고 명시되어 있으므로, 특정 디렉토리(`XUCL`에 해당하는)에 해당 파일을 관리하는 로직 필요. | 명령 사용자 제한 파일 제어. 시스템 재시작 후에 적용됨. |
| **CRTFRM** | 지원 가능 (UI/데이터 정의) | **고정 프레임 데이터/UI 요소 생성** <br> - `固定フレーム` (Fixed Frame)은 UI 레이아웃의 고정된 부분을 정의하는 것으로 추정. <br> - HTML/CSS 템플릿, 이미지 파일, 또는 UI 빌더에서 생성된 레이아웃 정의 파일로 매핑. <br> - Python의 `Jinja2` 또는 Java의 `Thymeleaf`와 같은 템플릿 엔진을 사용하여 프레임 정의 및 렌더링. | 고정 프레임 생성. |
| **DLTFRM** | 지원 가능 | **고정 프레임 데이터/UI 요소 삭제** <br> - `CRTFRM`으로 생성된 관련 파일을 삭제. | 고정 프레임 삭제. |
| **RNMFILE** | 지원 가능 | **파일 이름 변경** <br> - Linux의 `mv` 명령(Shell) 또는 언어별 파일 시스템 API(`os.rename()` in Python, `Files.move()` in Java) 사용. | 파일 이름 변경. |
| **CRTFILE** | 지원 가능 | **파일 생성** <br> - Linux의 `touch` 또는 `dd` 명령(Shell), 또는 언어별 파일 시스템 API(`open()`, `FileOutputStream`, `Path.createFile`) 사용. <br> - `SIZE` 파라미터는 `truncate` 또는 `dd` 명령으로 파일 크기 지정. | 일반 파일 생성. |
| **CLRFILE** | 지원 가능 | **파일 내용 지우기** <br> - Linux의 `truncate -s 0` 또는 `>` 명령(Shell), 또는 언어별 파일 시스템 API(`FileOutputStream` with `false` append)로 파일 내용을 비움. | 파일 내용 삭제. |
| **CHGFILE** | 지원 가능 | **파일 속성 변경** <br> - Linux의 `chmod` (권한), `chown` (소유자), `chgrp` (그룹) 명령(Shell) 또는 언어별 파일 시스템 API 사용. <br> - "파일 편성 변경"은 파일 내부 구조 변경을 의미할 수 있으므로, 데이터 마이그레이션 스크립트나 커스텀 변환 도구 필요. | 파일 속성 변경. |
| **DLTFILE** | 지원 가능 | **파일 삭제** <br> - Linux의 `rm` 명령(Shell) 또는 언어별 파일 시스템 API(`os.remove()` in Python, `Files.delete()` in Java) 사용. | 파일 삭제. |
| **DSPTLSPC** | 부분적으로 지원 가능 (파일 시스템 도구 활용) | **TMS 라이브러리 공간 사용량 표시** <br> - Linux의 `df`, `du` 명령(Shell) 또는 Python의 `os`, `shutil` 모듈을 사용하여 디렉토리/파일 시스템의 공간 사용량을 조회. <br> - "TMS 라이브러리"는 일반적인 파일 시스템의 특정 경로에 매핑될 것이므로, 해당 경로의 사용량 확인. | TMS 라이브러리 공간 사용량 표시. |
| **RCOVTMS** | 부분적으로 지원 가능 (백업/복구 시스템 연동) | **TMS 라이브러리 복구** <br> - Linux의 `rsync` 또는 `tar`와 같은 백업 도구를 사용하여 백업된 파일 시스템 스냅샷을 복원. <br> - "TMS 라이브러리"의 데이터베이스 메타데이터가 있다면, DB 복구 절차도 포함. | TMS 라이브러리 복구. |
| **RPRTMS** | 부분적으로 지원 가능 (파일 시스템/DB 일관성 검사) | **TMS 라이브러리 불일치 복구** <br> - 파일 시스템의 `fsck`와 유사한 도구 또는 데이터베이스의 일관성 검사 유틸리티를 사용하여 "TMS 라이브러리"의 메타데이터 불일치를 감지하고 복구하는 커스텀 스크립트/프로그램 개발. | TMS 라이브러리 불일치 복구. |
| **CRTTDIR** | 지원 가능 | **TMS 디렉토리 생성** <br> - Linux의 `mkdir` 명령(Shell) 또는 언어별 파일 시스템 API(`os.makedirs()` in Python, `Files.createDirectories()` in Java) 사용. <br> - `PATH` (슬래시 구분 경로)와 `TLIB` (다계층 라이브러리명)는 Linux 파일 시스템의 디렉토리 구조로 매핑. | 다계층 라이브러리에 디렉토리 생성. |
| **DLTTDIR** | 지원 가능 | **TMS 디렉토리 삭제** <br> - Linux의 `rmdir` 또는 `rm -r` 명령(Shell) 또는 언어별 파일 시스템 API(`os.rmdir()`/`shutil.rmtree()` in Python) 사용. | 다계층 라이브러리 디렉토리 삭제. |
| **CHGHDI** | 부분적으로 지원 가능 (홈 디렉토리 관리) | **프로필별 홈 디렉토리 정보 변경** <br> - Linux에서 사용자 계정의 홈 디렉토리는 `/etc/passwd` 또는 LDAP/Active Directory에 의해 관리됨. <br> - `usermod -d` 명령 (Shell) 또는 LDAP/DB API를 사용하여 사용자 프로필과 홈 디렉토리 경로를 업데이트. <br> - "프로필" 개념이 Linux 사용자 계정이나 애플리케이션 특정 프로필과 연동되어야 함. | 프로필별 홈 디렉토리 정보 변경. |
| **DLTHDI** | 부분적으로 지원 가능 (홈 디렉토리 정보 삭제) | **프로필별 홈 디렉토리 정보 삭제** <br> - `CHGHDI`와 유사하게, `/etc/passwd` 또는 LDAP/DB에서 해당 사용자 프로필의 홈 디렉토리 정보를 삭제. 실제 디렉토리 삭제는 별도로 관리. | 프로필별 홈 디렉토리 정보 삭제. |
| **MNTTLIB** | 부분적으로 지원 가능 (파일 시스템 마운트/심볼릭 링크) | **TMS 라이브러리 마운트** <br> - Linux의 `mount` 명령을 사용하여 특정 파일 시스템을 디렉토리에 마운트하거나, `ln -s` 명령으로 심볼릭 링크를 생성하여 논리적인 마운트 효과를 냄. <br> - `MODE` (`@R` 읽기 전용, `@RW` 읽기/쓰기)는 Linux의 마운트 옵션(`ro`, `rw`) 또는 파일 권한(`chmod`)으로 매핑. | 다계층 라이브러리 마운트. |
| **UMNTTLIB** | 부분적으로 지원 가능 (파일 시스템 언마운트/심볼릭 링크 삭제) | **TMS 라이브러리 언마운트** <br> - Linux의 `umount` 명령 또는 심볼릭 링크 삭제 (`rm`) 명령 사용. | 다계층 라이브러리 언마운트. |
| **DSPTMNT** | 부분적으로 지원 가능 (파일 시스템 마운트 정보 표시) | **TMS 마운트 상태 표시** <br> - Linux의 `mount` 명령 출력(`cat /proc/mounts`) 또는 `find -L` 명령을 통해 심볼릭 링크 정보를 조회하고, 이를 해석하여 표시. | TMS 마운트 상태 표시. |
| **ADDMNTI** | 부분적으로 지원 가능 (마운트 정보 설정) | **다계층 라이브러리 마운트 지정 정보 추가** <br> - 마운트 설정 정보를 저장하는 커스텀 설정 파일 (예: JSON, YAML) 또는 데이터베이스에 새로운 마운트 항목을 추가하는 스크립트/프로그램 개발. <br> - 이 정보는 `MNTTLIB` 명령에서 참조되어 실제 마운트 작업에 사용됨. | 다계층 라이브러리 마운트 지정 정보 추가. |
| **DLTMNTI** | 부분적으로 지원 가능 (마운트 정보 삭제) | **다계층 라이브러리 마운트 지정 정보 삭제** <br> - `ADDMNTI`로 추가된 설정 파일 또는 데이터베이스에서 해당 마운트 항목을 삭제. | 다계층 라이브러리 마운트 지정 정보 삭제. |
| **DSPMNTI** | 부분적으로 지원 가능 (마운트 정보 표시) | **다계층 라이브러리 마운트 지정 정보 표시** <br> - 마운트 설정 파일을 읽어와 파싱하고, 사용자에게 가독성 있는 형식으로 출력. | 다계층 라이브러리 마운트 지정 정보 표시. |
| **STUPTMS** | 부분적으로 지원 가능 (초기 환경 설정) | **다계층 파일 시스템 환경 설정** <br> - "TMS"는 가상 파일 시스템 개념으로 보이며, 특정 디렉토리 구조 생성, 권한 설정, 기본 파일/DB 생성 등 초기 환경을 설정하는 스크립트 (Shell) 또는 프로그램 (Python) 개발. | 다계층 파일 시스템 환경 설정. |
| **CRTSPF** | 지원 가능 | **특수 파일 생성** <br> - Linux의 `mknod` 명령을 사용하여 장치 파일 (`block` 또는 `character device`)을 생성하거나, 특정 애플리케이션에서 내부적으로 특별하게 취급하는 빈 파일을 생성. <br> - `/dev` 디렉토리와 유사한 개념으로 보임. | 특수 파일 생성. |
| **DSPTUSE** | 부분적으로 지원 가능 (리소스 사용량 모니터링) | **TMS 리소스 사용량 표시** <br> - `df`, `du`, `lsof` 등 Linux 시스템 리소스 모니터링 도구(Shell) 또는 `psutil` (Python)과 같은 라이브러리를 사용하여 디스크, 메모리, 프로세스 등 리소스 사용량 조회 및 표시. | TMS 리소스 사용량 표시. |
| **CPYTDIR** | 지원 가능 | **디렉토리 일괄 복사** <br> - Linux의 `cp -r` 명령(Shell) 또는 언어별 파일 시스템 API(`shutil.copytree()` in Python) 사용. | 디렉토리 일괄 복사. |
| **DSPVLDF** | 부분적으로 지원 가능 (파일 기술 정의 조회) | **VLD 파일 기술 표시** <br> - "VLD 파일 기술"은 특정 메시지 파일에 대한 메타데이터 정의로 추정. <br> - 이 정의를 저장하는 설정 파일(JSON, XML) 또는 데이터베이스 스키마를 설계하고, 해당 내용을 읽어와 포맷팅하여 표시. | VLD (비축적형 논리 대상) 파일 기술 표시. |
| **CRTACMF** | 부분적으로 지원 가능 (메시지 큐 정의 생성) | **ACM 파일 기술 생성** <br> - "ACM"은 메시지 큐와 관련된 파일 기술로 추정. <br> - 메시지 큐 시스템(예: RabbitMQ)의 큐, 교환기, 바인딩 등을 정의하는 구성 파일 또는 API를 통해 생성. <br> - Python의 `pika` (RabbitMQ 클라이언트) 또는 Java의 `JMS` API를 사용하여 큐 정의. | ACM (축적형 논리 대상) 파일 기술 생성. |
| **CHGACMF** | 부분적으로 지원 가능 (메시지 큐 정의 변경) | **ACM 파일 기술 변경** <br> - `CRTACMF`로 정의된 메시지 큐 시스템의 설정을 변경하는 API 또는 명령. | ACM 파일 기술 변경. |
| **DSPACMF** | 부분적으로 지원 가능 (메시지 큐 정의 조회) | **ACM 파일 기술 표시** <br> - `CRTACMF`로 정의된 메시지 큐 시스템의 현재 설정을 조회하고 표시. | ACM 파일 기술 표시. |
| **CTLMJS** | 지원 가능 (서비스 관리) | **MOMJ 서비스 제어** <br> - "MOMJ 서비스"는 특정 백그라운드 프로세스나 데몬으로 추정. <br> - Linux의 `systemctl` (systemd 서비스 제어) 또는 `service`, `init.d` 스크립트를 사용하여 서비스 시작, 중지, 재시작, 상태 확인 등을 제어. | MOMJ 서비스 제어. |
| **CHGCJMSD** | 부분적으로 지원 가능 (네트워크 환경 설정 변경) | **CJMS 네트워크 환경 정의 모듈 변경** <br> - "CJMS"는 Job Management System으로 추정. 네트워크 환경 정의는 원격 작업 제출, 스케줄링 등에 대한 설정. <br> - 분산 Job 스케줄러 (예: `Jenkins`, `Airflow`의 Job 정의, 또는 `Ansible` 설정)의 구성 파일을 변경하거나 API를 호출하여 설정 업데이트. <br> - `OSNAME` (`MSP`, `XSP`, `ASP`), `PTY` (우선순위), `SCHEDULE` (`@JOB`, `@FILE`), `FCBLIB`, `OVLLIB` 등 다양한 네트워크/작업 관련 파라미터를 Linux 환경의 적절한 설정으로 매핑. | CJMS 네트워크 환경 정의 모듈 변경. |
| **CRTCJMSH** | 지원 가능 (로그/히스토리 파일 생성) | **CJMS 히스토리 파일 생성** <br> - CJMS 작업 실행 이력을 저장할 로그 파일 또는 데이터베이스 테이블 생성. | CJMS 히스토리 파일 생성. |
| **DLTCJMSH** | 지원 가능 (로그/히스토리 파일 삭제) | **CJMS 히스토리 파일 삭제** <br> - `CRTCJMSH`로 생성된 히스토리 파일 또는 DB 테이블 삭제. | CJMS 히스토리 파일 삭제. |
| **DSPCJMSH** | 지원 가능 (로그/히스토리 조회) | **CJMS 히스토리 파일 표시** <br> - `CRTCJMSH`로 생성된 히스토리 파일 또는 DB 테이블의 내용을 조회하고 표시. | CJMS 히스토리 파일 표시. |
| **STRCMS** | 지원 가능 (서비스 시작) | **CM 서버 시작** <br> - `CTLMJS`와 유사하게, `systemctl` 또는 `service` 명령을 사용하여 "CM 서버" 데몬 시작. | CM 서버 시작. |
| **STRCDX** | 지원 가능 (서비스 시작) | **레지던트 타입 CORDEX 시작** <br> - `CORDEX`는 파일 전송 기능으로 추정됨 (`SNDXFILE`, `RCVXFILE`). <br> - SFTP 서버, rsync 데몬, 또는 커스텀 파일 전송 에이전트를 백그라운드 서비스로 시작 (`systemctl start`). | 레지던트 타입 CORDEX 시작. |
| **STPCDX** | 지원 가능 (서비스 중지) | **레지던트 타입 CORDEX 중지** <br> - `STRCDX`로 시작된 서비스 중지 (`systemctl stop`). | 레지던트 타입 CORDEX 중지. |
| **SNDXFILE** | 지원 가능 (파일 전송) | **CORDEX 파일 전송** <br> - `scp`, `sftp`, `rsync` 명령(Shell) 또는 언어별 네트워크/파일 전송 라이브러리 (Python의 `paramiko` for SFTP, `requests` for HTTP) 사용. | CORDEX를 통한 파일 전송. |
| **RCVXFILE** | 지원 가능 (파일 수신) | **CORDEX 파일 수신** <br> - `SNDXFILE`과 유사하게, `scp`, `sftp`, `rsync` 또는 웹 서버/API를 통해 파일 수신. | CORDEX를 통한 파일 수신. |
| **ACTWS** | 부분적으로 지원 가능 (워크스테이션 활성화) | **워크스테이션 활성화** <br> - 원격 데스크톱 (VNC, RDP), SSH 연결 또는 터미널 세션 활성화. <br> - Linux의 로그인 관리자(`display-manager` 서비스)를 제어하거나, 특정 사용자 세션을 활성화/비활성화. | 워크스테이션 활성화. |
| **ACTXLNK** | 부분적으로 지원 가능 (네트워크 연결 관리) | **XLINK 환경 개설** <br> - "XLINK"는 분산 시스템 간의 통신 프레임워크로 추정. <br> - VPN 연결, SSH 터널 또는 특정 네트워크 서비스(포트) 활성화. <br> - 방화벽 규칙(`firewalld`, `ufw`, `iptables`) 설정 변경. | XLINK 환경 개설. |
| **ADDCHR** | 부분적으로 지원 가능 (폰트/문자 셋 관리) | **사용자 정의 문자 등록** <br> - 시스템의 폰트 설정 (예: `fontconfig`), 터미널 에뮬레이터의 글꼴 설정 또는 애플리케이션 내의 사용자 정의 문자 매핑 테이블에 새로운 문자 정의 추가. | 사용자 정의 문자 등록. |
| **ADDDPCS** | 부분적으로 지원 가능 (분산 프로세스 관리) | **DPCS 리소스 동적 추가** <br> - "DPCS"는 분산 프로세스 제어 시스템으로 추정. <br> - 컨테이너 오케스트레이션 (Docker Compose, Kubernetes) 또는 Job 스케줄러 (Slurm, LSF)를 통해 분산 리소스 (프로세스, 서비스)를 동적으로 배포/추가. | DPCS 리소스 동적 추가. |
| **ADDHDI** | 부분적으로 지원 가능 (홈 디렉토리 정보 등록) | **프로필별 홈 디렉토리 정보 등록** <br> - `CHGHDI`와 유사하게, 사용자 프로필의 홈 디렉토리 정보를 생성/등록. | 프로필별 홈 디렉토리 정보 등록. |
| **CANPGM** | 지원 가능 (프로세스 종료) | **프로그램 중단** <br> - Linux의 `kill` 명령(Shell) 또는 `os.kill()`(Python)을 사용하여 특정 프로세스 ID (PID)를 가진 프로그램을 종료. <br> - `PID`를 식별하는 메커니즘 필요. | 프로그램 강제 종료. |
| **CANRPSYN** | 부분적으로 지원 가능 (데이터 동기화 중단) | **레플리케이션 동기화 실행 중지** <br> - 데이터베이스 레플리케이션 (PostgreSQL Streaming Replication, MySQL Group Replication) 또는 파일 동기화 (rsync, unison) 프로세스를 중단. <br> - 해당 동기화 서비스/데몬에 중지 신호를 보내거나 프로세스 종료. | 레플리케이션 동기화 중단. |
| **CANWTR** | 지원 가능 (프린터 라이터 중단) | **라이터 중단** <br> - CUPS 프린터 큐의 작업을 중지하거나, 스풀링된 작업을 제거. `lprm` 명령 등 사용. | 프린터 라이터 중단. |
| **CANXJOB** | 지원 가능 (원격 작업 중단) | **원격 작업 중단** <br> - `ACTXLNK`와 유사하게, 원격 시스템에서 실행 중인 특정 Job을 종료하는 명령을 전송하거나, 해당 원격 시스템의 프로세스 ID를 식별하여 종료. | XLINK 작업을 중단. |
| **CHARM** | 부분적으로 지원 가능 (문자/글꼴 관리) | **사용자 정의 문자 관리 유틸리티** <br> - `ADDCHR`와 유사하게, 문자 셋 또는 글꼴 관련 설정을 관리하는 유틸리티. <br> - Linux의 `fontconfig` 설정 파일을 편집하거나, `iconv`와 같은 문자 인코딩 변환 도구를 활용. | 사용자 정의 문자 관리 유틸리티. |
| **CHGCMVAR** | 부분적으로 지원 가능 (환경 변수/설정 변경) | **공통 변수 변경** <br> - 시스템 전역 또는 사용자 세션의 환경 변수를 설정(`export`, `/etc/environment`, `.profile`)하거나, 애플리케이션에서 사용하는 공통 설정 파일 (예: `/etc/my_app.conf`)을 변경. | 공통 변수 변경. |
| **CHGCPTF** | 부분적으로 지원 가능 (CPT 파일 기술 변경) | **CPT 파일 기술 변경** <br> - "CPT 파일"은 통신 프로토콜 관련 파일 기술로 추정. <br> - 소켓 통신 (TCP/IP) 설정, 메시지 큐 통신 파라미터 등을 변경. <br> - `DEV` (장치 명), `WAIT` (메시지 유닛 획득 대기) 등은 소켓 연결/읽기 타임아웃, 비동기 I/O 등으로 매핑. | CPT (통신 프로그램용 파일) 파일 기술 변경. |
| **CHGDIAL** | 부분적으로 지원 가능 (네트워크 다이얼 설정 변경) | **다이얼 번호 변경** <br> - 모뎀 연결, VPN 다이얼업 또는 네트워크 게이트웨이 설정 변경. <br> - `pppd` 설정 파일 또는 VPN 클라이언트 설정 파일 편집. | 다이얼 번호 변경. |
| **CHGLIBL** | 부분적으로 지원 가능 (라이브러리 검색 경로 변경) | **라이브러리 리스트 변경** <br> - 프로그램/모듈 검색 경로 (환경 변수 `LD_LIBRARY_PATH`, `PYTHONPATH`, `CLASSPATH` 등)를 변경. <br> - 애플리케이션이 사용하는 라이브러리 목록을 동적으로 변경하는 기능을 제공해야 함. <br> - `MODE` (`@ADD`, `@DLT`)에 따라 추가/삭제. | 라이브러리 검색 순서 변경. |
| **CHGMQD** | 부분적으로 지원 가능 (메시지 큐 정의 변경) | **메시지 큐 기술 변경** <br> - `CRTMQD`로 생성된 메시지 큐의 속성 (예: 메시지 수준 `MSGLEVEL`, 발신원 정보 `UPI`, 우선순위 `UPR`) 변경. <br> - 메시지 브로커 (RabbitMQ)의 관리 API 또는 설정 파일을 통해 큐 속성 변경. | 메시지 큐 기술 변경. |
| **CHGNETID** | 부분적으로 지원 가능 (네트워크 식별자 변경) | **네트워크 식별자 변경** <br> - 호스트 이름 (`hostnamectl`), IP 주소, 서브넷 마스크, 게이트웨이 등 네트워크 인터페이스 설정을 변경. <br> - `/etc/hostname`, `/etc/network/interfaces` 또는 `NetworkManager` 명령 (`nmcli`) 사용. | 네트워크 식별자 변경. |
| **CHGOPRD** | 부분적으로 지원 가능 (운영 환경 정의 변경) | **운영 환경 기술 변경** <br> - 운영 시스템의 동작 방식에 대한 광범위한 설정 변경. <br> - 메시지 표시 방식 (`RMSGMOD`, `EMSGMOD`), UI 관련 모드 (`MNUGDMOD`, `SYSNMOD`, `JOBNMOD` 등), 최대 워크스테이션 Job 수 (`MAXWSJB`) 등을 설정. <br> - `systemd` 서비스 설정, 애플리케이션의 전역 설정 파일, 또는 환경 변수를 변경하는 스크립트/프로그램 개발. | 운영 환경 기술 변경. |
| **CHGOWN** | 지원 가능 | **엔티티 소유권 변경** <br> - Linux의 `chown` 명령(Shell) 또는 언어별 파일 시스템 API (`os.chown()` in Python)를 사용하여 파일/디렉토리의 소유자(사용자/그룹) 변경. <br> - "엔티티"는 Linux에서 파일, 디렉토리, 프로세스 등을 의미. | 엔티티 소유권 변경. |
| **CHGPF** | 부분적으로 지원 가능 (물리 파일 정보 변경) | **물리 파일 정보 변경** <br> - "물리 파일"은 파일 시스템의 실제 파일에 해당. <br> - 파일의 속성 (예: 백업 여부 `BACKUP`, 저널링 `JOURNAL`), 스택(`STACK`)은 파일의 데이터 저장 방식이나 접근 패턴에 대한 메타데이터로 추정. <br> - Linux 파일 시스템의 메타데이터 직접 변경은 어렵지만, 파일 시스템 확장 속성 (`setfattr`) 또는 애플리케이션 DB에서 관리하는 방식으로 구현. | 물리 파일 정보 변경. |
| **CHKRPULG** | 지원 가능 (로그 상태 확인) | **레플리케이션 업데이트 정보 로깅 상태 확인** <br> - 데이터베이스 레플리케이션 로그 (예: PostgreSQL WAL, MySQL Binary Log)의 상태를 확인하거나, rsync와 같은 파일 동기화 도구의 로그 파일 상태를 파싱. | 레플리케이션 업데이트 정보 로깅 상태 확인. |
| **CHKRWD** | 지원 가능 (원격 창 기능 확인) | **원격 창 기능 판정** <br> - 원격 데스크톱 또는 VNC/RDP 서비스가 실행 중인지 확인 (`ps aux | grep`, `systemctl status`). <br> - 원격 세션이 활성 상태인지 확인. | 원격 창 기능 판정. |
| **CHKSTS** | 지원 가능 (상태 확인) | **상태 판정 (시스템 또는 Job)** <br> - 시스템 상태 (`uptime`, `free`, `ps`, `df` 등 Linux 명령) 또는 특정 Job의 상태 (`ps`, `systemctl status`, Job 스케줄러 API)를 확인하고, 결과를 반환 (`@PGMEC`, `@PGMBOOL`). <br> - `TYPE` 파라미터에 따라 `DA` (디스크 파일 존재 여부), `SWITCH` (인디케이터 상태), `JOBKEY` (Job 식별 기호 등록 여부) 등 다양한 상태 확인. | 시스템 또는 Job 상태 판정. |
| **CHKSYSWK** | 지원 가능 (시스템 작업 영역 확인) | **시스템 작업 영역 사용 상황 백업 사이즈 참조** <br> - Linux의 `/tmp` 또는 특정 애플리케이션의 작업 디렉토리 공간 사용량을 `du -sh` 명령(Shell) 또는 Python의 `shutil.disk_usage()`로 확인. | 시스템 작업 영역 사용량 확인. |
| **CHKWACC** | 지원 가능 (계정 정보 확인) | **계정 정보 무결성 체크** <br> - 사용자 계정 데이터베이스 (예: LDAP, FreeIPA) 또는 웹 애플리케이션의 사용자 테이블에서 계정 정보의 유효성/무결성 (비밀번호 정책, 계정 잠김 등) 확인. | 웹 계정 정보 무결성 체크. |
| **CHSET** | 부분적으로 지원 가능 (문자 셋 관리) | **문자 셋 관리 유틸리티** <br> - 시스템의 언어/로케일 설정(`localectl`), 문자 인코딩 변환 (`iconv`), 또는 폰트 관리 (`fontconfig`)를 위한 유틸리티. | 문자 셋 관리 유틸리티. |
| **CL** | 지원 가능 | **제어 언어 프로그램 번역** <br> - "제어 언어 프로그램"은 쉘 스크립트 (Shell script) 또는 Python 스크립트와 유사한 개념으로 추정. <br> - 쉘 스크립트 인터프리터 (`bash`), Python 인터프리터 또는 Java 컴파일러를 사용하여 해당 스크립트/프로그램을 번역 (컴파일)하여 실행 가능한 형태로 생성. <br> - `OBJLIB` (목적 프로그램 라이브러리), `REP` (대체), `PUBAUT` (공적 인가 정보), `SRCL` (소스 리스트), `ERRL` (진단 메시지 리스트), `SXREFL` (상호 참조 리스트) 파라미터는 컴파일러/인터프리터의 옵션에 매핑. | 제어 언어 프로그램 번역. |
| **CLRACM** | 부분적으로 지원 가능 (메시지 큐 메시지 삭제) | **ACM 내 저장 메시지 삭제** <br> - `CRTACMF`에서 언급된 메시지 큐 (예: RabbitMQ) 내의 특정 큐 또는 모든 큐에 쌓인 메시지를 삭제. <br> - 메시지 브로커의 관리 API (예: RabbitMQ HTTP API)를 통해 큐 비우기. | ACM 내 저장 메시지 삭제. |
| **CLRELIST** | 부분적으로 지원 가능 (추출 정보 초기화) | **DB-EXPRESS 추출 정보 리스트 초기화** <br> - 데이터 추출/변환 서비스에서 사용하는 임시 데이터 또는 설정 정보를 초기화. <br> - 데이터베이스 테이블의 `TRUNCATE` 또는 관련 캐시/설정 파일 삭제. | DB-EXPRESS 추출 정보 리스트 초기화. |
| **CLRTLIB** | 부분적으로